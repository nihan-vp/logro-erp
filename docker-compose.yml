version: "3.8"

services:
  logro:
    build: .
    container_name: logro-app
    restart: always
    ports:
      - "9098:5000"
    env_file:
      - .env
    environment:
      - NODE_ENV=production
